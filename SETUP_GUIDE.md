# M5Stack Core2 + ENV III セットアップ手順書

明日、実機（Core2本体・ENV III Unit）が届いたらすぐに動かせるように、
「今夜のうちにPCで済ませておくこと」と「届いてからやること」を分けてあります。
プログラムコード（stage1 / stage2）は前回渡した分をそのまま使います。

---

## 0. 今夜のうちにPCで終わらせておくこと（実機なしでOK）

これを済ませておくと、明日は配線して書き込むだけになります。

### 0-1. Arduino IDEのインストール

- https://www.arduino.cc/en/software から、自分のOS（Windows/Mac）用の最新版（IDE 2.x系）をダウンロードしてインストール

### 0-2. USBドライバ（CP210x）のインストール

Core2はUSBシリアル変換に **CP2104** というチップを使っています。PCがCore2を「COMポート」として認識するために、Silicon Labs公式のドライバを入れておきます。

- ダウンロード: https://www.silabs.com/developer-tools/usb-to-uart-bridge-vcp-drivers （Windows/Macそれぞれ用がある）
- インストール後は一度PCを再起動しておくと安全です
- ※すでに他のM5StackやESP32ボードを使ったことがあり、認識実績があるなら不要な場合もあります

### 0-3. Arduino IDEにM5Stackのボード情報を追加

1. Arduino IDEを開き、`File > Preferences`（Macは`Arduino IDE > Settings`）を開く
2. 「Additional boards manager URLs」欄に以下を追加
   ```
   https://static-cdn.m5stack.com/resource/arduino/package_m5stack_index.json
   ```
3. OKで閉じる
4. 左サイドバーの「ボードマネージャ」（基板のアイコン）を開き、`M5Stack` で検索してインストール（数分かかります）

### 0-4. 必要なライブラリのインストール

左サイドバーの「ライブラリマネージャ」で以下を検索してインストール。

- `M5Unified`（M5Stack公式）
- `M5Unit-ENV`（M5Stack公式。SHT30/QMP6988用）

### 0-5. ボードの選択（実機がまだなくても設定だけ先にできます）

`Tools > Board > M5Stack > M5Stack Core2` を選択しておく。

### 0-6. コードを手元に用意

前回渡した以下のファイルを、デスクトップなど分かりやすい場所に置いておく。

- `stage1_env_display/stage1_env_display.ino`
- `stage2_kobae_monitor/stage2_kobae_monitor.ino`

ここまでが今夜できる準備です。

---

## 1. 実機が届いたら：開封チェック

箱の中身を確認します。

- [ ] M5Stack Core2 本体
- [ ] ENV III Unit（緑色の小さい基板、SHT30+QMP6988搭載）
- [ ] Groveケーブル（4pin、両端が白いコネクタ）※ENV IIIに付属していることが多いですが、なければ別途「Groveケーブル」で検索して用意してください
- [ ] USB Type-Cケーブル（データ通信対応のもの。充電専用ケーブルだとPCに認識されないので注意）

---

## 2. 配線

1. ENV III Unit と Core2 を Groveケーブルで接続する
   - Core2の側面（Port A、黄色/白のGroveコネクタ）に挿す
   - Port Aは `SDA=G32, SCL=G33` に固定配線されているので、コード側の設定変更は不要
2. Core2 を USB Type-C ケーブルで PC に接続する

---

## 3. Stage 1: 温湿度表示の確認（まずはここだけ）

1. Arduino IDE で `stage1_env_display.ino` を開く
2. `Tools > Board` が `M5Stack Core2` になっているか確認
3. `Tools > Port` で、Core2に対応するCOMポート（Windowsは`COM3`など、Macは`/dev/cu.usbserial-xxxx`や`/dev/cu.SLAB_USBtoUART`など）を選択
   - ポートが一覧に出てこない場合は「0-2のドライバ」がうまく入っていない可能性が高いです（後述のFAQ参照）
4. 左上の「→（マイコンボードに書き込む）」ボタンを押す
5. 書き込みが終わると自動的に再起動し、画面に "ENV III ready" と出た後、温度・湿度が1秒おきに表示されればOK
6. うまくいかない場合は `Tools > Serial Monitor`（ボーレート115200）を開いてエラーメッセージを確認

**ここまで動けば、センサーと配線とPC側の設定はすべて正しいということです。**

---

## 4. Stage 2: キャラ・危険度・タッチ演出の確認

1. Arduino IDE で `stage2_kobae_monitor.ino` を開く（ボード/ポート設定はStage 1のままでOK）
2. 書き込む
3. 画面上部に `STATUS: SAFE`（起動直後は経過時間0なので必ずSAFEになります）、右側にキャラの顔、右下に `CLEANED!` ボタンが出ることを確認
4. `CLEANED!` ボタンをタップして、以下が起きることを確認
   - 短い振動
   - 「ピロン」という3音の効果音（**Core2本体下部のスピーカーミュートスイッチがOFF側になっていないか確認**）
   - お皿がゴミ箱アイコンへ飛んでいくアニメーション
   - バナーが `GOOD JOB!` になり、キャラが喜び顔になる（約1.5秒後に元に戻る）

危険度のしきい値や倍率を試しに変えたい場合は、コード冒頭の `TEMP_HIGH_C` / `RISK_TH_*` などの定数を書き換えて再度書き込みます。

---

## 5. トラブルシューティングFAQ

**Q. Tools > Port に何も出てこない / COMポートが見当たらない**
- USBケーブルがデータ通信対応か確認（充電専用ケーブルは認識されません）
- CP210xドライバが入っているか確認（0-2）。入れてもダメならPCを再起動
- 別のUSBポート（PC本体直挿し。ハブ経由は避ける）に挿し直す

**Q. 書き込み時に "Failed to connect to ESP32" のようなエラーが出る**
- Core2本体の電源ボタンを長押しして再起動してから再度書き込みを試す
- USBケーブル・ポートを変える
- それでもダメなら、書き込み開始直後に本体右側面の電源ボタンを1回押す（手動でリセットをかけるタイミングを合わせる）とうまくいくことがあります

**Q. "ENV III (SHT30) not found!" と表示される**
- ENV IIIがPort A（Groveの黄/白コネクタ）に挿さっているか確認
- Groveケーブルの向き・挿し込みが甘くないか確認
- 別のGroveケーブルに交換して試す

**Q. 音が出ない**
- Core2本体下部にあるスピーカーのミュートスライドスイッチを確認
- `M5.Speaker.setVolume()` の値を上げてみる（コード内で調整可能）

**Q. 振動しない**
- Core2 v1.0とv1.1でバイブレーションモーターの個体差があるという報告があるため、コード上は正しくても本体側の問題の可能性があります。まずは温湿度・キャラ表示・音が正常なことを優先し、振動は後回しでも発表には支障ありません

**Q. コンパイルエラーが出る**
- エラーメッセージ全文を送ってもらえれば、すぐに原因を切り分けます

---

## 6. 明日の作業の目安タイムライン

| 作業 | 目安時間 |
|---|---|
| 開封・付属品確認 | 5分 |
| 配線（Groveケーブル接続） | 2分 |
| Stage 1 書き込み・温湿度表示確認 | 10分 |
| Stage 2 書き込み・キャラ/危険度/タッチ演出確認 | 15分 |
| しきい値や演出の微調整 | 任意 |

準備さえ0章で終わっていれば、実機が届いてから30分程度で一通り動く状態まで持っていけるはずです。
