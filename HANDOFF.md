# 引き継ぎメモ

## プロジェクトの到達点

「ぬりかえす」は、台所の放置リスクをインクで覆われる画面として可視化し、指で拭く操作を片付け行動へ結び付けるM5Stack Core2のプロトタイプです。

本プロジェクトは**100programのファイナルへ進出**しました。イベント向け開発は一区切りついており、現在は成果保存・引き継ぎ段階です。

## 最終構成

- 最終候補の実機コード: `stage3_ink_firebase_monitor_2.ino`
- インク体験の単体版: `nurikaesu_ink_3.ino`
- スマホWeb画面: `kitchen-hazard-monitor.html`
- 公開URL: https://kobaesystem.web.app
- Firebase: Authentication、Realtime Database、Hostingを使用

最終候補には、温湿度取得、4段階の危険度、キャラクター、インク演出、ワイプ操作、音、振動、バッテリー表示、Firebase送信が統合されています。

## 確認済みの基盤

- Core2をPCから認識し、Arduino IDEから書き込める
- ENV IIIから温度・湿度を取得できる
- Core2の画面、タッチ、音、振動が動作する
- FirebaseプロジェクトとWebアプリが公開されている
- Web画面にFirebase AuthenticationのログインとRealtime Database監視が実装されている

## リポジトリだけでは最終確認を断定できない点

最後に追加された`stage3_ink_firebase_monitor_2.ino`について、コンパイル成功・実機書き込み・Firebaseまでの通し試験結果は文書に残っていません。再開する場合は、ここを最初に確認してください。

1. 最終候補をArduinoの同名フォルダへ置く
2. `secrets.h`を同じフォルダへ用意する
3. `M5Unified`、`M5Unit-ENV`、`FirebaseClient`をインストールする
4. Core2へ書き込む
5. シリアルモニターでWi-Fi接続とFirebaseエラーを確認する
6. Realtime Databaseの`devices/core2-01/latest`が約10秒ごとに更新されることを確認する
7. スマホ画面へログインし、実測値が表示されることを確認する

## 既知の制約

- 最終候補はデモ速度で、実時間1分をシステム上の1時間として扱う
- Core2を再起動すると、最終候補の片付け経過時間は0へ戻る
- スマホ側の「生ごみを捨てた」はFirebaseへ保存されるが、Core2はその値を読み戻していない
- 危険度通知のプッシュ通知は未実装
- Firebaseルールは試作用で、ログイン利用者は全端末データへアクセスできる
- 危険度は啓発用の簡易モデルで、医学・食品衛生上の判定には使えない

## 秘密情報の扱い

以下はGitHubや公開資料へ載せず、必要な担当者へ別経路で渡してください。

- 使用場所のWi-Fi名とパスワード
- Firebaseの試作用メールアドレスとパスワード
- Firebase Consoleの管理権限

Firebase管理権限はGoogleアカウント自体を貸さず、Firebaseプロジェクトの「ユーザーと権限」から担当者を追加してください。`secrets.h`はGit管理対象外です。

## 再開時の優先順位

1. 最終候補のコンパイルと実機通し試験
2. デモ速度を通常速度へ戻すか判断
3. 再起動後も片付け時刻を保持する
4. スマホとCore2の片付け状態を双方向同期する
5. 必要ならプッシュ通知を追加する

## 引き継ぎ相手へ送る短い説明

> M5Stack Core2と温湿度センサーを使い、台所の放置リスクを画面を覆うインクとして可視化する「ぬりかえす」という試作です。指でインクを拭くと片付け完了になり、Firebase経由でスマホから温湿度も確認できます。100programのファイナル進出作品です。最終候補はstage3_ink_firebase_monitor_2.inoで、再開時は最初に実機とFirebaseの通し確認をしてください。
